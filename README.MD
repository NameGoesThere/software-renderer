# TODO
* Stop using the painters algorithm.
* Surface normals + lighting.
* Optimize .obj loading to not call `realloc()` every iteration.
* Multi-color models.
* Better transformation system.
* Z-buffering.